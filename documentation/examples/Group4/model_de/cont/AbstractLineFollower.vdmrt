class AbstractLineFollower
	values
		protected SAMPLE_TIME = 0.02;

-- TODO Define instance variables here
	operations
		protected Step: () ==> ()
		Step() == is subclass responsibility;

	thread periodic(40E6, 0 ,0, 0)(Step);

end AbstractLineFollower