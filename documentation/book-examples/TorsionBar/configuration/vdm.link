-- linking of the monitored variables
input enc_motor = TorsionBar.ctrl.EncMotor.mvalue;
input enc_load = TorsionBar.ctrl.EncLoad.mvalue;

-- linking of the controlled variables
output pwm_amp = TorsionBar.ctrl.PWM.cvalue;

-- linking of the shared design parameters
sdp strategy = World.sid;

-- other linked names used in scenarios
--model mode = System.ctrl.mode
